{
  "name": "@drf/tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "bench:limit": "vitest run --config vitest.bench.config.ts"
  },
  "dependencies": {
    "@drf/anchor-client": "workspace:^",
    "@drf/api": "workspace:^",
    "@drf/db": "workspace:^",
    "@drf/shared": "workspace:^",
    "drizzle-orm": "^0.44.7"
  },
  "devDependencies": {
    "@drf/web": "workspace:^",
    "@hono/node-server": "^1.19.9",
    "playwright": "^1.62.1",
    "vite": "^8.2.2"
  }
}
